<button 
  hx-get="/builds/recent"
  hx-target="#recent-builds"
  hx-swap="innerHTML"
  class="bg-blue-500 text-white px-4 py-2 rounded">
  Load Recent Builds
</button>

<div id="recent-builds" class="mt-4"></div>
